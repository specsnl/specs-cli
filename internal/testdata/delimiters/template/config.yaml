# Standard GitHub Actions syntax passes through unchanged with custom delimiters:
name: ${{ github.repository }}
ref: ${{ github.ref }}

# Rendered with custom delimiters:
project: [[ .ProjectName ]]
